# Navigate playlist
LEFT     playlist-prev
RIGHT    playlist-next

# h / l: skip backward / forward by 1 second
h seek -1
l seek 1

# n / p: step forward / backward by exactly one frame
n frame-step
p frame-back-step

# Toggle on-screen playlist (requires osc)
y        script-message osc playlist
